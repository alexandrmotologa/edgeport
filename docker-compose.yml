version: "3.9"

services:
  edgeport-relay:
    build: .
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    command: ["serve", "--host", "0.0.0.0", "--port", "8000", "--domain", "localhost"]
